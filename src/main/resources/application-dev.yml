spring:
  devtools:
    devtools:
      add-properties: "true"
    restart:
      enabled: "true"

logging:
  level:
    org:
      hibernate:
        SQL: "DEBUG"
      type:
        descriptor:
          sql:
            BasicBinder: "TRACE"
